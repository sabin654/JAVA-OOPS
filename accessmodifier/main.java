package accessmodifier;

public class main {

	public static void main(String[] args) {
		// public which means all acces in - class , package, subclass which is extends class , world
		//private which means only access in class , but no package, no subclass and no world
		//protected which means access in class, package and sub class but no world
		//default which mean  access in class and package but no subclass and world 
		
	}

}
